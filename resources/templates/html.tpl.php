<!DOCTYPE html>
<html lang="{{language}}" dir="{{language_dir}}">
<head>
    {{head}}
    <title>{{head_title}}</title>
    {{styles}}
    {{scripts}}
</head>
<body>
{{page_top}}
{{page}}
{{page_bottom}}
</body>
</html>